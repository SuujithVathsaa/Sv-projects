# Higgsfield API credentials — copy to .env.local and fill in.
# Get them from https://cloud.higgsfield.ai/
#
# .env.local is gitignored. Never commit it, and never paste the key
# into a chat or a terminal that logs its input.
HF_KEY="your-key-id:your-key-secret"
